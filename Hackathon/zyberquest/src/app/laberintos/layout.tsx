export const revalidate = 0;                  
export const dynamic = 'force-dynamic';
export const fetchCache = 'default-no-store';

export default function LaberintosLayout({ children }: { children: React.ReactNode }) {
  return children;
}