[package]
name = "zecauth-ffi"
version = "0.1.0"
edition = "2021"
description = "ZecAuth C FFI — bridge for Kotlin (JNI) and Swift wallet SDKs"
license = "MIT"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
zecauth-core = { path = "../zecauth-core" }
serde_json = "1"
hex = "0.4"
