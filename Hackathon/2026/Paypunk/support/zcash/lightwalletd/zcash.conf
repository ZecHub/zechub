# Read by lightwalletd to reach zcashd's RPC (the container name resolves on the
# docker network). lightwalletd auto-detects regtest from getblockchaininfo.
rpcuser=zcashrpc
rpcpassword=notsecure
rpcconnect=zcashd
rpcport=18232
