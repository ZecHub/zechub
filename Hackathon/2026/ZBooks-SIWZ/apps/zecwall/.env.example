# Same shape as ZBooks. The two apps can share a service address or run
# entirely separately — that's the point of SIWZ as a primitive.
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3001
SIWZ_DOMAIN=localhost:3001
SIWZ_SERVICE_ADDRESS=
SIWZ_DEMO=1
