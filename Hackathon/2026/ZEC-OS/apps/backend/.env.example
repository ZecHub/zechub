# ZEC-OS — environment. Fill these in; never commit real secrets.
# DATABASE_URL: a Postgres connection string (required — auth is server-side).

DATABASE_URL=
PORT=
FASTIFY_URL=
# Requires Zebra 5.2.x + indexer/parser/API for live chain data (see README)
ZEC_API_URL=
API_SECRET_KEY=
ZAINO_GRPC_URL=
ZAINO_GRPC_API_KEY=
BACKEND_ZADDR=
BACKEND_TADDR=
ZINGO_EXEC=
ADMIN_HANDLE=
SESSION_EXPIRY_HOURS=
